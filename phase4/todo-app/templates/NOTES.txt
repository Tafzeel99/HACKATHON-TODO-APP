╔══════════════════════════════════════════════════════════════════════╗
║          Phase 4 Todo App - Successfully Deployed! 🚀                ║
╚══════════════════════════════════════════════════════════════════════╝

Thank you for installing {{ .Chart.Name }} version {{ .Chart.Version }}!

📦 Deployed Components:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{- if .Values.backend.enabled }}
  ✅ Backend (FastAPI + SQLModel + AI)
{{- end }}
{{- if .Values.frontend.enabled }}
  ✅ Frontend (Next.js + ChatKit)
{{- end }}

🔗 Access Your Application:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{{- if .Values.frontend.enabled }}
{{- if eq .Values.frontend.service.type "NodePort" }}

1. Get the Minikube IP:

   minikube ip

2. Access the Frontend:

   http://<MINIKUBE-IP>:{{ .Values.frontend.service.nodePort }}

   Example:
   http://192.168.49.2:{{ .Values.frontend.service.nodePort }}

3. Access the AI Chat:

   http://<MINIKUBE-IP>:{{ .Values.frontend.service.nodePort }}/chat

{{- end }}
{{- end }}

{{- if .Values.backend.enabled }}
{{- if eq .Values.backend.service.type "NodePort" }}

4. Access the Backend API Docs:

   http://<MINIKUBE-IP>:{{ .Values.backend.service.nodePort }}/docs

5. Backend Health Check:

   curl http://<MINIKUBE-IP>:{{ .Values.backend.service.nodePort }}/health

{{- end }}
{{- end }}

⚠️  IMPORTANT - Port Forwarding for ChatKit:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

OpenAI ChatKit requires 127.0.0.1 (not minikube IP). Use port forwarding:

1. Forward Frontend to 127.0.0.1:3000:

   kubectl port-forward svc/{{ include "todo-app.fullname" . }}-frontend 3000:3000

2. Forward Backend to 127.0.0.1:8000:

   kubectl port-forward svc/{{ include "todo-app.fullname" . }}-backend 8000:8000

3. Then access:

   Frontend: http://127.0.0.1:3000
   Chat:     http://127.0.0.1:3000/chat
   Backend:  http://127.0.0.1:8000/docs

📊 Check Status:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

View all resources:

  kubectl get all -l app.kubernetes.io/instance={{ .Release.Name }}

View pods:

  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}

View logs:

  kubectl logs -l app.kubernetes.io/component=backend -f
  kubectl logs -l app.kubernetes.io/component=frontend -f

Describe deployment:

  kubectl describe deployment {{ include "todo-app.fullname" . }}-backend
  kubectl describe deployment {{ include "todo-app.fullname" . }}-frontend

🔧 Troubleshooting:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If pods are not running:

  1. Check events:
     kubectl get events --sort-by=.metadata.creationTimestamp

  2. Check pod status:
     kubectl describe pod -l app.kubernetes.io/instance={{ .Release.Name }}

  3. Check logs:
     kubectl logs -l app.kubernetes.io/component=backend --tail=50
     kubectl logs -l app.kubernetes.io/component=frontend --tail=50

If images can't be pulled:

  Make sure you built the images and loaded them into Minikube:

  minikube image load todo-backend:4.0.0
  minikube image load todo-frontend:4.0.0

🎯 Next Steps:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Wait for pods to be ready:

   kubectl wait --for=condition=ready pod -l app.kubernetes.io/instance={{ .Release.Name }} --timeout=300s

2. Set up port forwarding for ChatKit (recommended):

   kubectl port-forward svc/{{ include "todo-app.fullname" . }}-frontend 3000:3000 &
   kubectl port-forward svc/{{ include "todo-app.fullname" . }}-backend 8000:8000 &

3. Access the application:

   http://127.0.0.1:3000

4. Test the features:
   - Register/Login
   - Create tasks
   - Test AI chat at http://127.0.0.1:3000/chat

📚 Documentation:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For more information, check:

  phase4/RUNNING_CONTAINERS.md - Container documentation
  phase4/README.md - Project overview

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Happy Task Managing with AI! 🎉
